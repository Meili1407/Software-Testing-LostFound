meta {
  name: 1. List Notifications (Student)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/notifications
  body: none
  auth: none
}

headers {
  x-user-id: {{studentId}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
